# Challenge

## Hints

- `const` correctness

  - `displayBalance()` const method must track the balance access count
  - Apply the `mutable` keyword only when necessary

- Friend relationship
  - Establish the missing `friend` relationship
  - The Auditor class must access private BakAccount member variables


