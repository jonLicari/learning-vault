
IoC pattern is about providing any kind of callback instead of acting ourself directly. The callback is what implements and/or controls reaction to an event. IoC inverts, or, redirects control to an external handler or controller.
## IoC without using [[Dependency Injection]]:

An example would be the Template pattern because the implementation can only be changed through sub-classing.
